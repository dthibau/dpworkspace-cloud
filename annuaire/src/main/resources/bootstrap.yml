spring:
  application:
    name: annuaire
    
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      
      
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
